#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    // vector<int> list;
    // list.push_back(1);
    // list.push_back(2);
    // list.push_back(3);
    // vector<int>::iterator p = list.begin();
    // cout << *p << endl;
    // p++;
    // cout << *p << endl;

    // vector<int> a={1,5,8,9,4,6,3};
    // sort(a.begin(),a.begin()+7);
    // for(int i = 0;i<7;i++)
    // {
    //     printf("%d\n",a[i]);
    // }

    // nth_element(a.begin(),a.begin()+3,a.end());
}
// struct Pos
// {
//     int x,y;
// };
// Pos pos[114514];

// bool cmp(pos a,pos b)
// {
//     if(a.x!=b.x)
//     {
//         return a.x < b.x;
//     }
//     return a.y < b.y;
// }