#include <bits/stdc++.h>
using namespace std;

int main()
{
    int len;
    cin >> len;
    int a[3];
    cin >> a[0] >> a[1] >> a[2];
    sort(a[0],a[2]);
    int count = 0;
    for(int i = 0;i<3;i++)
    {
        
    }
}